version: "2"

run:
  timeout: 5m
  go: "1.25.0"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
    - misspell
    - unused
    - gocritic

formatters:
  enable:
    - gofmt
