# WI-0002: ccc Bootstrap — Copilot Config CLI Application

## Summary

Scaffold and implement the `ccc` (Copilot Config CLI) Go application — a TUI tool that reads `~/.copilot/config.json`, auto-detects the Copilot CLI config schema, and presents settings in an interactive terminal UI for viewing and editing.

## Status

Research
